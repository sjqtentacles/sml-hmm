(* hmm.mlb

   MLB for the sml-hmm library: the vendored sml-matrix sources plus the hidden
   Markov model sources.

   The dependency .mlb file is brought in WITHOUT a `local` wrapper so that
   consumers (the test suite, examples) can name `Matrix` alongside `Hmm`. *)
$(SML_LIB)/basis/basis.mlb
../lib/github.com/sjqtentacles/sml-matrix/sources.mlb
hmm.sig
hmm.sml
